<UserControl x:Class="Quasar.Views.SlotListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="500">
    <Grid Background="{StaticResource CutieBG08}" Height="30" Width="auto" Margin="5,1" DataContext="{Binding SlotViewModel}">
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="55"/>
            <ColumnDefinition Width="175"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Style="{StaticResource CutieTextBlockStyle}" VerticalAlignment="Center" FontSize="14" Margin="8,0" >
            <Run Text="Slot "/><Run Text="{Binding SlotNumber}"/>
        </TextBlock>
        <TextBlock Grid.Column="1" Style="{StaticResource CutieTextBlockStyle}" VerticalAlignment="Center" FontSize="14" Margin="8,0" >
            <Run Text="{Binding TypeName}"/>
        </TextBlock>
        <TextBlock Grid.Column="12" Style="{StaticResource CutieTextBlockStyle}" VerticalAlignment="Center" FontSize="14" Margin="8,0" >
            <Run Text="{Binding ContentName}"/>
        </TextBlock>
    </Grid>
</UserControl>
