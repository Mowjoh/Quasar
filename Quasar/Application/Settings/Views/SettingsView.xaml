<UserControl x:Class="Quasar.Settings.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:p="clr-namespace:Quasar.Properties"
             DataContext="{Binding SVM}">

    <Grid Background="{DynamicResource CutieBG01}">

        <!--  Settings Panel  -->
        <Grid Background="{DynamicResource CutieBG01}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0"
                  Margin="15,0,15,15"
                  Background="{DynamicResource CutieBG01}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="185" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <!--  Application Settings Grid  -->
                <Grid Grid.Row="0" Background="Transparent">
                    <Label Margin="0,5,5,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                           Content="{x:Static p:Resources.Settings_ApplicationSettingsHeader}" FontSize="15" Style="{StaticResource CutieLabelStyle}" />
                    <ItemsControl ItemsSource="{Binding AppSettings}" Margin="0,40,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <Button Width="300" Height="32" Margin="3,135,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                            Command="{Binding Path=MoveInstallCommand}" Content="{x:Static p:Resources.Settings_MoveInstallFolderContent}" Template="{StaticResource CutieSmallButtonTemplate}" />
                    <Path Width="30" Height="30" Margin="320,140,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                          Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 17h-2v-2h2v2zm2.07-7.75l-.9.92C13.45 12.9 13 13.5 13 15h-2v-.5c0-1.1.45-2.1 1.17-2.83l1.24-1.26c.37-.36.59-.86.59-1.41 0-1.1-.9-2-2-2s-2 .9-2 2H8c0-2.21 1.79-4 4-4s4 1.79 4 4c0 .88-.36 1.68-.93 2.25z" Fill="White" Stroke="White" />

                    <!--<Label Margin="0,135,5,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                           Content="This enables you to move your Quasar Library to another folder" FontSize="13" Style="{StaticResource CutieLabelStyle}" />-->
                </Grid>

                <!--  FTP Settings Grid  -->
                <Grid Grid.Row="1" Background="Transparent">
                    <Label Margin="0,5,5,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                           Content="{x:Static p:Resources.Settings_FtpSettingsHeader}" FontSize="15" Style="{StaticResource CutieLabelStyle}" />
                    <ItemsControl ItemsSource="{Binding FTPSettings}" Margin="0,35,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <Button Width="300" Height="32" Margin="3,218,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                            Command="{Binding Path=ValidateFTPCommand}" Content="{x:Static p:Resources.Settings_ValidateFTPContent}" Template="{StaticResource CutieSmallButtonTemplate}" />
                    <Path Width="30" Height="30" Margin="320,223,0,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                          Data="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 17h-2v-2h2v2zm2.07-7.75l-.9.92C13.45 12.9 13 13.5 13 15h-2v-.5c0-1.1.45-2.1 1.17-2.83l1.24-1.26c.37-.36.59-.86.59-1.41 0-1.1-.9-2-2-2s-2 .9-2 2H8c0-2.21 1.79-4 4-4s4 1.79 4 4c0 .88-.36 1.68-.93 2.25z" Fill="White" Stroke="White" />

                    <!--<Label Margin="270,180,5,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                           Content="When you've typed your address and authentication info, use this to enable FTP when transferring" FontSize="12" Style="{StaticResource CutieLabelStyle}" />-->
                </Grid>



            </Grid>
            <Grid Grid.Column="1" Background="{StaticResource CutieBG06}" />
            <Grid Grid.Column="2"
                  Margin="15,0,0,0"
                  Background="{DynamicResource CutieBG01}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="190" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <!--  Transfer Settings  -->
                <Grid Grid.Row="0" Background="Transparent">
                    <Label Margin="0,5,5,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                           Content="{x:Static p:Resources.Settings_FileTransferHeader}" FontSize="15" Style="{StaticResource CutieLabelStyle}" />
                    <ItemsControl ItemsSource="{Binding TransferSettings}" Margin="0,35,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Grid>

                <Grid Grid.Row="1" Background="Transparent">
                    <Label Margin="0,5,5,0" HorizontalAlignment="Left" VerticalAlignment="Top"
                           Content="{x:Static p:Resources.Settings_TogglesHeader}" FontSize="15" Style="{StaticResource CutieLabelStyle}" />
                    <ItemsControl ItemsSource="{Binding WarningSettings}" Margin="0,35,0,0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </Grid>


            </Grid>
        </Grid>





    </Grid>

</UserControl>
