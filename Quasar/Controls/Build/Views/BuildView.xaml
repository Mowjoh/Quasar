<UserControl x:Class="Quasar.Controls.Build.Views.BuildView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quasar.Controls.Build.Views"
             mc:Ignorable="d" 
             d:DesignHeight="620" d:DesignWidth="1120">
    <Grid Background="{DynamicResource QD1}">
        <Grid.RowDefinitions>
            <RowDefinition Height="69*"/>
            <RowDefinition Height="55*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="Transfer Settings" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}"/>
        <GroupBox Header="Transfer Options" HorizontalAlignment="Stretch" Height="250" Margin="10,50,10,0" VerticalAlignment="Top"  Foreground="{DynamicResource QuasarTextColor}">
            <Grid>
                <RadioButton Content="Local Transfer (SD)" IsChecked="{Binding LocalSelected}" GroupName="TransferOption" HorizontalAlignment="Left" Margin="209,13,0,0" VerticalAlignment="Top" Foreground="{DynamicResource QuasarTextColor}"/>
                <RadioButton Content="Wireless Transfer (FTP)" IsChecked="{Binding WirelessSelected}" GroupName="TransferOption" HorizontalAlignment="Left" Margin="10,13,0,0" VerticalAlignment="Top" Foreground="{DynamicResource QuasarTextColor}"/>
                <ComboBox Margin="330,10,34,0" VerticalAlignment="Top" ItemsSource="{Binding Drives}" SelectedItem="{Binding SelectedDrive}" DisplayMemberPath="Name"/>
                <Button x:Name="Minimize" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,13,10,0">
                    <Button.Template>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Path x:Name="Minimize" Stroke="White" Fill="White" Data="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z" Margin="10,0,0,0" Height="15" Width="15"  Stretch="Uniform" ToolTip="Refresh the drive list" />
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Label Content="Mod Loader :" HorizontalAlignment="Left" Margin="10,93,0,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Width="89"/>
                <ComboBox HorizontalAlignment="Left" Margin="104,93,0,0" VerticalAlignment="Top" Width="235" ItemsSource="{Binding ModLoaders}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedModLoader}"/>
                <RadioButton Content="Wipe and Recreate" IsChecked="{Binding WipeSelected}" GroupName="TransferMode" HorizontalAlignment="Left" Margin="233,198,0,0" VerticalAlignment="Top" Foreground="{DynamicResource QuasarTextColor}"/>
                <RadioButton Content="Comparative Mode" IsChecked="{Binding ComparativeSelected}" GroupName="TransferMode" HorizontalAlignment="Left" Margin="10,198,0,0" VerticalAlignment="Top" Foreground="{DynamicResource QuasarTextColor}"/>
                <Label Content="How do you want Quasar to handle the file transfer ?" HorizontalAlignment="Left" Margin="10,169,0,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Width="329"/>
            </Grid>
        </GroupBox>
        <ScrollViewer Grid.Column="1" Grid.RowSpan="2" Background="#87000000">
            <TextBlock TextWrapping="Wrap" Text="{Binding Logs}" Foreground="{DynamicResource QuasarTextColor}"/>
        </ScrollViewer>
        <Button Content="Send my mods to my Switch" HorizontalAlignment="Center" VerticalAlignment="Top" Width="184" Margin="188,310,188,0" Command="{Binding Path=BuildCommand}"/>
        <Label Content="Transfer Status :" HorizontalAlignment="Center" Margin="0,10,0,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Grid.Row="1"/>
        <ProgressBar  Value="20" Height="10" Margin="10,115,10,0" Grid.Row="1" VerticalAlignment="Top"/>
        <Label Content="Step" HorizontalAlignment="Left" Margin="10,46,0,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Grid.Row="1" Width="420"/>
        <Label Content="Sub-Step" HorizontalAlignment="Left" Margin="10,75,0,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Grid.Row="1" Width="420"/>
        <Label Content="20/100" HorizontalAlignment="Center" Margin="253,140,260,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Grid.Row="1"/>
        <Label Content="5,3 MB/s" HorizontalAlignment="Center" HorizontalContentAlignment="Right" Margin="435,46,5,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Grid.Row="1" Width="120"/>
        <Label Content="15 MB / 37,4 MB" HorizontalAlignment="Center" HorizontalContentAlignment="Right" Margin="435,75,5,0" VerticalAlignment="Top" Style="{DynamicResource QuasarLabel}" Grid.Row="1" Width="120"/>
        <StackPanel Orientation="Horizontal" Margin="0,10,10,0" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Label Content="Active Workspace :"  Style="{DynamicResource QuasarLabel}"/>
            <Label Content="{Binding ActiveWorkspace.Name}"  Style="{DynamicResource QuasarLabel}"/>
        </StackPanel>
        

    </Grid>
</UserControl>
